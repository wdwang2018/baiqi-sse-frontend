# 白起SSE 商用版 · 阶段性日志

**生成日期**：2026-07-26
**记录人**：WorkBuddy（开发主力）
**阶段定位**：MVP → 商用版迁移完成，云端闭环跑通

---

## 一、当前状态（一句话）

MVP 单 HTML 文件已演进为 **Next.js 全栈商用版**；AI端（`python_service` FastAPI）已上云 Render，前端（母舰）已切云端，完整调用链跑通：

```
母舰(前端 localhost:3000) ──HTTP──▶ AI端(AI服务 Render) ──DEEPSEEK_API_KEY──▶ 大模型(DEEPSEEK) ──话术──▶ 母舰
```

沙箱侧配置 / 契约已静态核验全绿；live 连通性需用户在浏览器 F12→Network 实测（Render 免费版冷启动可能超时，多试一次）。

---

## 二、术语规范（2026-07-26 起生效，权威）

| 名称 | 含义 | 位置 / 域名 |
|------|------|------------|
| **母舰** | 前端（Next.js 编排层，用户点的界面） | `localhost:3000` |
| **AI端 / AI服务** | FastAPI 微服务（源码 `python_service/`） | 本地 `192.168.1.75:8001` 兜底 / 云端 `sse-ai-backend.onrender.com` |
| **大模型** | 真正生成话术的生成引擎 | `api.deepseek.com` |

> ⚠️ 历史记忆（2026-07-25 及之前）中的「母舰」曾反向指代现在的「AI服务」，以本节为准。

---

## 三、架构演进时间线

| 日期 | 里程碑 | 关键说明 |
|------|--------|----------|
| 2026-04-20 | MVP 设计稿 | IBM SSM 方法 + 单 HTML（7314 行），localStorage，浏览器直连 LLM |
| 2026-07-06 | 商用版脚手架 | Next.js 14 + React 18 + Prisma 6 + Supabase + NextAuth v5；默认账号 `admin@baiqi.ai / admin123` |
| 2026-07-07 | 九宫图模块迁移 | 4 步向导 / AI 生成 / 3×3 网格 / 历史记录 / 保存导出 |
| 2026-07-13 | 终极闭环架构 | 新增 `Project`（基础物科库）+ `ProjectModuleData`（万能结果库 JSONB）；九宫图改用「项目闭环」版本目录，不覆盖 |
| 2026-07-19 / 20 | 项目汇总库 + 7 阶段引擎 | `ProjectInteraction` 实时写入；7 阶段 AI 大脑迁出 Next.js，前端直连 Python `/classify`；统一 `buildProjectSnapshot()` |
| 2026-07-24 / 25 | AI端上云 Render | `python_service` 重写为部署版（契约对齐 + 跨域可选鉴权 + `render.yaml`/gunicorn/部署指南），独立 git 仓库 |
| 2026-07-26 | 前端切云端 + 术语规范 + 修复 | 三处前端调用改读 `NEXT_PUBLIC_API_URL`；固化术语；修复双 `next dev` 冲突导致的 `.next` 损坏 |

---

## 四、当前运行架构

### 4.1 调用链
```
浏览器 → 母舰(localhost:3000)
            │  fetch /api/ai/nine-box        {toolType, input{project_id在内部}}
            │  fetch /api/ai/projects/classify  {projectId, projectSnapshot: JSON字符串}
            ▼
        AI端(sse-ai-backend.onrender.com, FastAPI)
            │  持 DEEPSEEK_API_KEY 调用
            ▼
        大模型(DEEPSEEK) ──话术──▶ AI端 ──{nine_grid_data / classify_data, usage}──▶ 母舰
```

### 4.2 关键文件
- 前端配置：`baiqi-sse/.env` → `NEXT_PUBLIC_API_URL="https://sse-ai-backend.onrender.com"`
- 前端三处调用：`src/app/(dashboard)/projects/[id]/page.tsx`（classify）、`nine-grid/page.tsx`、`module/pipeline/page.tsx`（nine-box）均读 `NEXT_PUBLIC_API_URL || "http://192.168.1.75:8001"`
- AI端源码：`baiqi-sse/python_service/`（13 文件，独立 git 仓库，已部署 Render）

---

## 五、关键接口契约（已 curl 实测，勿凭记忆改）

### 5.1 九宫图 / Pipeline（nine-box）
- **请求**：`{ toolType: str, input: { project_id, customer, industry, contact, contactTitle, pain, products } }`
  - 注意：`project_id` 在 `input` **内部**，不在外层。
- **响应**：`{ nine_grid_data: { block_1..9, citations[] }, usage: { prompt/completion/total_tokens, estimated_cost_usd } }`

### 5.2 7 阶段研判（classify）
- **请求**：`{ projectId: str, projectSnapshot: str（JSON 字符串，AI端会再 json.loads 一次） }`
  - 发对象会 400；发 JSON 字符串 → 200。
- **响应**：`{ classify_data: { suggestedStage: int(1-7), stageEvidence: str }, usage: { tokens, cost } }`
  - 无 stageLabel / confidence 包装。

### 5.3 鉴权（当前「放开」模式）
- 优先级：X-API-Key → NextAuth Cookie → 默认租户。
- `AI_API_KEY` 留空时直接放行（降级默认租户），返回 200。
- 若 Render 侧设 `AI_API_KEY` 加固，前端三处 fetch 需改带 `X-API-Key` 头（读 `NEXT_PUBLIC_API_KEY`），否则跨域 401。

---

## 六、已知限制与风险

| 风险 | 说明 | 应对 |
|------|------|------|
| 沙箱无出网 | 开发侧连 google 也 000，无法 live ping Render | 联动性靠用户在浏览器实测 |
| Render 免费版冷启动 | 首次点击可能超时 30–60s | 多试一次或后台唤醒 |
| 跨域 Cookie 不跨域 | localhost:3000 的 NextAuth Cookie 不会发往 render.com | 鉴权放开（AI_API_KEY 留空）；如需加固改用 X-API-Key |
| 双 dev server 冲突 | 同一项目跑两个 `next dev` 会互相踩 .next 致 vendor chunk 损坏 | 只保留一个 dev server；启动加 `env -u CODEBUDDY_SESSION_ID -u CLAUDE_SESSION_ID` 绕过 safe-delete 守卫 |
| 本地 AI端 不可达 | 192.168.1.75:8001 为局域网，仅兜底，沙箱不可探测 | 用户需保证本机/局域网真实 AI端 已启动 |

---

## 七、下一步

1. **模块扩展**：按同架构扩展价值主张（value-prop）/ 战情Lab（battle-lab）等「厨师」模块到 AI端。
2. **鉴权加固**：生产环境为 Render 配 `AI_API_KEY`，前端 fetch 改带 `X-API-Key` 头。
3. **多租户**：明确租户隔离策略，替代当前「默认租户」降级。
4. **正式环境**：Supabase 连接串从开发库切换到生产库；Render 升级付费版规避冷启动。
5. **可观测性**：AI端加上 token/成本用量看板，呼应 `usage` 字段。

---

*本日志由 WorkBuddy 依据项目记忆与 2026-07-26 当日工作记录汇总生成。详细流水见 `.workbuddy/memory/2026-07-26.md`。*

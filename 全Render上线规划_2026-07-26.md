# 白起SSE · 全 Render 上线规划

> 生成日期：2026-07-26
> 适用场景：前端（母舰）从本地 `localhost:3000` 迁到云端，客户仅需浏览器 + 账号密码
> 拓扑决策：**先全 Render 验证，后迁国内**（AI端 已在 Render；前端新建 Render Web Service）

---

## 0. 决策背景

- **目标**：前端上云，客户浏览器打开域名 → 登录即用，零安装。
- **已确认事实**：AI端（python_service / FastAPI）已部署 Render `https://sse-ai-backend.onrender.com`；数据库 Supabase PostgreSQL 已在云端。
- **客户地域**：国内为主。
- **最终拓扑**：先以「前端 Render + AI端 Render」全栈跑通验证；若国内延迟不可接受，再整体迁国内云（方案 C）。
- **现状利好**：前端 `src/lib/auth.ts` 已设 `trustHost: true` → 部署到 Render **无需改任何前端代码**，只配环境变量 + 建 Web Service。

---

## 1. 目标拓扑

```
客户浏览器（国内/海外）
      │  https://<前端>.onrender.com
      ▼
前端 Render Web Service（Next.js, $PORT）
      │  NEXT_PUBLIC_API_URL →
      ├──────────────────────────────▶  AI端 Render（FastAPI, 已上云）
      │                                      │  DEEPSEEK_API_KEY
      │                                      ▼
      │                                  大模型 DeepSeek
      │  DATABASE_URL →
      ▼
Supabase PostgreSQL（已云端，切生产库）
```

---

## 2. 前置准备（需你提供 / 确认）

- [ ] GitHub 仓库已连接 Render（前端源码 `baiqi-sse` 可被 Render 拉取）
- [ ] Supabase **生产库** 连接串（`DATABASE_URL`）——当前 `.env` 是 dev 库，上线需切换到生产库
- [ ] 生成强随机 `NEXTAUTH_SECRET`：`openssl rand -base64 32`
- [ ] 前端 Render 分配到的域名（如 `https://baiqi-sse.onrender.com`），用于 `NEXTAUTH_URL` 与 AI端 CORS

---

## 3. 步骤一：前端 Render Web Service

### 3.1 后台手动操作
1. Render 控制台 → **New** → **Web Service** → 连接仓库 `baiqi-sse`
2. Name：`baiqi-sse-frontend`
3. Runtime：**Node**
4. Build Command：`npm install && npm run build`
5. Start Command：`npm run start`
6. 端口：留空（Render 自动注入 `$PORT`，`next start` 默认读取）
7. 实例：免费版会休眠（验证足够）；正式建议付费保活
8. 部署完成后填下面的环境变量

### 3.2 等效 `render.yaml`（也可直接在后台导入）

```yaml
services:
  - type: web
    name: baiqi-sse-frontend
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: NEXTAUTH_SECRET
        sync: false        # 后台手动填，勿入库
      - key: NEXTAUTH_URL
        sync: false        # 填前端 Render 域名
      - key: DATABASE_URL
        sync: false        # 填 Supabase 生产库
      - key: NEXT_PUBLIC_API_URL
        value: https://sse-ai-backend.onrender.com
      - key: NEXT_PUBLIC_API_KEY
        sync: false        # 验证期留空；正式前填并前端 fetch 带 X-API-Key
```

> 注：`postinstall` 脚本已配 `prisma generate`，构建时会自动生成客户端，无需手动加步骤。

---

## 4. 步骤二：环境变量表

| 变量 | 值 / 来源 | 是否密钥 | 说明 |
|------|-----------|----------|------|
| `NODE_VERSION` | `22` | 否 | 用受管 Node 22 |
| `NEXTAUTH_SECRET` | `openssl rand -base64 32` | **是** | JWT / 会话签名，必须强随机 |
| `NEXTAUTH_URL` | 前端 Render 域名 | 否 | `trustHost:true` 已设可省，但显式更稳 |
| `DATABASE_URL` | Supabase **生产库** | **是** | 切生产库，勿用 dev 库当生产 |
| `NEXT_PUBLIC_API_URL` | `https://sse-ai-backend.onrender.com` | 否 | 保持 AI端 不变 |
| `NEXT_PUBLIC_API_KEY` | 验证期空 | **是** | 正式前启用 `X-API-Key` 加固 |

---

## 5. 步骤三：AI端 CORS 放行前端域名

- 在 `python_service` 的 `FRONTEND_ORIGIN` 环境变量中**增加前端 Render 域名**（逗号分隔，保留 `http://localhost:3000` 便于本地联调）。
- 若源码已是 `allow_origins = FRONTEND_ORIGIN.split(",")` 的环境变量驱动方式 → **只改 Render 后台环境变量即可，无需改代码重部署**。
- 验证期 `AI_API_KEY` 留空（放开模式）；正式前设 `AI_API_KEY` + 前端三处 fetch 加 `X-API-Key` 头（读 `NEXT_PUBLIC_API_KEY`），否则跨域会 401。

---

## 6. 步骤四：数据库迁移

- 本地或 CI 执行：`npx prisma migrate deploy`（目标 = 生产库 `DATABASE_URL`）
- 初始化账号：`npx prisma db seed`（生成 `admin@baiqi.ai / admin123`）
- ⚠️ 首次用生产库建议用 `migrate deploy`（受控），不要 `db push` 直接覆盖；确认 schema 与 dev 库一致。

---

## 7. 步骤五：验证 Checklist

- [ ] 前端域名可打开，无 500 错误
- [ ] `/login` 可用 `admin@baiqi.ai` 登录
- [ ] 九宫图生成返回真实内容（非桩文字）
- [ ] 7 阶段 `classify` 返回 `suggestedStage` / `stageEvidence`
- [ ] 浏览器 F12 → Network：AI端 请求 200、无 CORS 红字、无 401
- [ ] 国内同事实测「打开页面 + AI 调用」延迟，记录是否可接受

---

## 8. 风险与缓解

| 风险 | 说明 | 缓解 |
|------|------|------|
| 冷启动 | Render 免费版休眠，首次点击超时 30–60s | 多试一次 / 付费保活 |
| 国内延迟 | 全栈海外，客户国内打开 + AI 调用跨洋 | 实测；不可接受则启动迁国内云预案 |
| 密钥管理 | `NEXTAUTH_SECRET` / `DATABASE_URL` 敏感 | 仅在 Render 后台设，勿提交仓库 |
| DB 污染 | 误用 dev 库当生产 | 明确切生产库连接串 |

---

## 9. 后迁国内云预案（按需启动）

若验证期国内延迟不可接受：
1. 前端 + AI端 都 Docker 化，部署阿里云 / 腾讯云容器服务
2. 域名解析到国内节点 + 国内 SSL 证书
3. DeepSeek 国内直连，延迟最优
4. 此预案在验证期后视延迟结果启动，**不影响当前全 Render 验证**

---

## 10. 回滚

- 若上线失败：Render 后台可删除 Web Service，或切回本地 `npm run dev`；AI端 不变。
- 本地前端代码未改动（`trustHost` 已就绪），随时可回本地运行。

---

*本规划由 WorkBuddy 依据 2026-07-26 部署讨论生成。架构细节见 `阶段日志_2026-07-26.md`。*
